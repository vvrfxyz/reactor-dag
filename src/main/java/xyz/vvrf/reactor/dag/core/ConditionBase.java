package xyz.vvrf.reactor.dag.core;

/**
 * 所有条件接口的基础标记接口。
 * 用于在 EdgeDefinition 中统一存储不同类型的条件。
 *
 * @param <C> 上下文类型
 * @author ruifeng.wen
 */
public interface ConditionBase<C> {
    // 这是一个标记接口，没有方法。
}
